// Admin Editing System \n// Features: Password login, Edit mode toggle, Inline editing, LocalStorage persistence \n\nconst adminPassword = 'securePassword'; // Change this to your actual password\nlet isAdmin = false;\n\nfunction login() {\n    const passwordInput = prompt('Enter the admin password:');\n    if (passwordInput === adminPassword) {\n        isAdmin = true;\n        localStorage.setItem('isAdmin', 'true');\n        alert('Login successful!');\n        toggleEditMode();\n    } else {\n        alert('Incorrect password.');\n    }\n}\n\nfunction toggleEditMode() {\n    const editElements = document.querySelectorAll('.editable');\n    editElements.forEach(el => {\n        el.contentEditable = isAdmin;\n        el.style.border = isAdmin ? '1px solid blue' : '';\n    });\n}\n\nfunction saveChanges() {\n    const editableContent = document.querySelectorAll('.editable');\n    editableContent.forEach(el => {\n        localStorage.setItem(el.id, el.innerHTML);\n    });\n    alert('Changes saved!');\n}\n\nfunction loadChanges() {\n    const editableContent = document.querySelectorAll('.editable');\n    editableContent.forEach(el => {\n        el.innerHTML = localStorage.getItem(el.id) || '';\n    });\n}\n\ndocument.addEventListener('DOMContentLoaded', (event) => {\n    if (localStorage.getItem('isAdmin') === 'true') {\n        isAdmin = true;\n    }\n    loadChanges();\n});\n\ndocument.getElementById('loginButton').addEventListener('click', login);\ndocument.getElementById('saveButton').addEventListener('click', saveChanges);